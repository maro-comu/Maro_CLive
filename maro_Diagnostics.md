# CLive_Maro 진단 안내

오류코드는 이 설명이나 컴파일러 문서를 엽니다. 행·열은 해당 코드로 이동합니다. 밑줄 해결책은 안내된 변경만 적용하며 Ctrl+Z로 되돌릴 수 있습니다. 일반 설명은 클릭해도 코드를 바꾸지 않습니다.

자동 수정은 모든 오류를 해결하는 기능이 아닙니다. 현재 원문·버전·파일·예상 문자열이 모두 일치하고 지원하는 코드 형태일 때만 제공됩니다. 비교 의도나 주소 출력 의도처럼 선택이 필요한 해결책은 그 조건을 읽고 적용하세요. 적용 후 다시 진단하므로 다른 독립 오류는 남을 수 있습니다.

CLive의 추가 검사는 작은 범위의 소스 패턴 분석입니다. 컴파일러의 전체 의미 분석을 대체하지 않습니다. 알 수 없는 헤더·조건부 컴파일·복잡한 선언에서는 추측해서 수정하지 않습니다. 외부 헤더 내부, 다른 번역 단위, 실행 중 모든 경로까지 검사한다는 뜻은 아닙니다.

처음 만든 파일은 한 번 저장한 뒤 사용하세요. 저장된 파일에서 아직 저장하지 않은 편집 내용은 진단과 수정에 반영됩니다. 생성 코드의 위치나 원본 문서를 확인할 수 없는 진단은 위치 이동·수정을 제공하지 않습니다.

## MARO-MACRO-VALUE

배열 크기 등 값이 필요한 자리에서 `#define MAX_SIZE = 100;`를 사용하면 `=`와 `;`까지 펼쳐져 문법 오류가 됩니다. 이 경우 사용한 행이 아니라 매크로 정의를 고칩니다.

해결책: `#define MAX_SIZE 100`처럼 값만 남깁니다. 코드 조각을 의도적으로 붙이는 다른 종류의 매크로는 이 규칙으로 임의 수정하지 않습니다. 확인된 같은 원인의 연관 진단만 접고 독립 오류는 유지합니다.

## MARO-MACRO-PRECEDENCE

`#define SQUARE(x) x * x`는 `SQUARE(2 + 3)`을 `2 + 3 * 2 + 3`으로 펼칩니다. 호출자의 예상과 연산 순서가 달라질 수 있습니다.

해결책: `#define SQUARE(x) ((x) * (x))`처럼 인수와 전체 식을 괄호로 감쌉니다. 이 수정은 인수가 두 번 평가되는 문제를 없애지 않습니다. `SQUARE(i++)`나 부작용이 있는 함수 호출은 피하고, 한 번만 계산해야 한다면 적절한 형식의 함수로 바꾸세요.

## MARO-LOCAL-LIFETIME

일반 지역 배열의 주소는 함수가 끝난 뒤 사용할 수 없습니다. `int local_arr[3]; return local_arr;`를 호출한 쪽에서 역참조하면 유효하지 않은 저장 공간에 접근할 수 있습니다.

해결 방향: 호출자가 준비한 배열과 크기를 인수로 받아 채우거나, 수명이 보장되는 값을 반환하도록 함수를 설계하세요. `static`은 공유 상태를 만들고 동적 할당은 해제 책임을 추가하므로 자동으로 선택하지 않습니다.

## MARO-STRING-COMPARE

문자 포인터와 문자열 리터럴을 `==`로 비교하면 내용이 아니라 주소를 비교합니다. 우연히 같은 주소여서 맞는 것처럼 보일 수도 있습니다.

내용 비교가 목적이면 유효한 널 종료 문자열에 대해 `strcmp(str1, "Hello") == 0`으로 바꾸세요. 자동 수정은 필요한 헤더와 알려진 문자열 상태를 확인한 단순한 경우에만 제공합니다. 주소 자체를 비교하려는 코드라면 이 해결책을 적용하지 마세요.

## MARO-ARRAY-ASSIGNMENT

`char str2[10]; str2 = str1;`처럼 배열 전체에 `=`로 대입할 수는 없습니다.

해결책: 대상 배열 크기 안에서 문자와 마지막 `\0`을 복사합니다. CLive는 변경되지 않은 문자열 리터럴의 길이와 수정 가능한 대상 배열 크기를 확인한 경우에만 길이가 정해진 `memcpy`를 제안합니다. 입력으로 길이가 달라지는 문자열은 길이 검사·초과 처리·종료 문자 공간을 직접 정해야 합니다. 더 작은 배열에 무조건 복사하는 수정은 제공하지 않습니다.

## MARO-PRINTF-POINTER

`printf("%d", iptr)`의 `%d`는 `int` 값을 요구하지만 `iptr`는 포인터입니다. 포인터를 정수처럼 전달하면 서식과 인수 형식이 맞지 않습니다.

주소를 출력하려는 경우 `%p`와 `(void*)iptr`로 바꾸는 해결책을 제공합니다. 가리키는 값을 출력하려는 경우에는 먼저 포인터가 유효하고 올바른 형식의 객체를 가리키는지 확인해야 합니다. 단순히 `*iptr`로 바꾸는 자동 수정은 위험하므로 제공하지 않습니다.

## MARO-POINTER-TYPE

`double pi`의 주소를 `int*`로 바꿔도 실수가 정수로 변환되는 것은 아닙니다. 다른 형식으로 역참조하면 언어 규칙을 위반할 수 있습니다.

해결 방향: 실수를 출력하려면 `printf("%f", pi)`처럼 원래 값과 형식을 사용하세요. 정수 값이 필요하면 범위와 소수 처리 방침을 확인한 뒤 값을 변환하세요. 바이트 표현 관찰이 목적이면 바이트 접근 규칙을 따르는 별도 구현이 필요합니다. 의도를 추측해 자동 변경하지 않습니다.

## MARO-CONDITION-ASSIGNMENT

`if (b = 0)`는 `b`에 0을 넣고 그 결과로 조건을 판정하므로 해당 조건은 거짓이 됩니다.

비교하려던 경우 `if (b == 0)`으로 바꾸는 해결책을 누르세요. 의도한 대입이라면 적용하지 말고 `(b = value) != 0` 등 의도한 조건을 명시하세요. CLive는 모든 `=`를 `==`로 바꾸지 않습니다.

## MARO-DIVIDE-ZERO

나누는 값이 0으로 확인된 식을 실행하면 정수 나눗셈이 유효하지 않습니다. 표시된 위치는 이 위험을 확인한 피연산자 위치이며, 전체 실행 경로를 증명했다는 뜻은 아닙니다.

해결 방향: 나눗셈 전에 분모를 검사하고 0일 때의 처리를 정하세요. 0을 임의로 1로 바꾸거나 계산을 삭제하지 않습니다. 앞의 `if (b = 0)` 같은 대입 오류가 원인일 수도 있으므로 해당 조건부터 확인하세요.

## MARO-MISSING-SEMICOLON

단순한 `return 0` 문장 끝에서 누락된 `;`의 위치를 확인한 경우입니다. 해결책을 누르면 그 위치에만 `;`를 넣습니다.

컴파일러의 C2143이 항상 세미콜론 누락을 뜻하지는 않습니다. 매크로·자료형·괄호 문제라면 그 원인을 먼저 고쳐야 하므로 확인되지 않은 위치에는 삽입하지 않습니다.

## 컴파일러 진단

CLive의 추가 규칙 외에도 MSVC 진단에 문제와 수정 방향을 안내합니다. 예를 들어 C2065는 선언·철자·범위를, C2664는 인수 형식을, C6386은 버퍼 크기와 쓰는 길이를 확인하도록 안내합니다. 알려지지 않은 진단은 내용을 꾸며내지 않고 컴파일러 설명을 유지합니다.

오류코드를 누르면 [Microsoft C/C++ 진단 문서](https://learn.microsoft.com/ko-kr/cpp/error-messages/compiler-errors-1/c-cpp-build-errors?view=msvc-170) 검색 또는 [Clang 경고 문서](https://clang.llvm.org/docs/DiagnosticsReference.html)로 연결됩니다. 소스 코드는 이 링크로 전송하지 않습니다.
