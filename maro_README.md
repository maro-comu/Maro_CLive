# CLive_Maro

1. Visual Studio를 닫고 `maro_CLive_Maro_v2.3.5.exe`를 설치한 뒤 C/C++ 문서를 엽니다.
2. 편집을 1~3초 멈추면 자동 분석·실행합니다. 왼쪽은 실시간 진단, 오른쪽은 라이브러리·헤더파일·함수·변수, 아래는 실시간 출력입니다. **오류코드**는 설명, **행·열**은 소스 위치를 엽니다. **밑줄 해결책**은 문구 어디를 눌러도 안내된 수정을 적용합니다. `직접 수정`은 자동 수정이 없는 안내입니다. Ctrl+Z로 되돌릴 수 있으며 원문이 바뀌면 적용하지 않습니다. 오른쪽 목록도 해당 위치로 이동합니다.
3. 입력 코드가 있을 때 나타나는 입력칸에서 Enter로 입력하고 Ctrl+D로 입력을 끝냅니다. 단일 입력은 전송 후 숨기고 반복·복합 입력은 유지합니다. 프로젝트·외부 헤더 등 판단이 어려운 경우에는 입력을 막지 않도록 유지합니다. 아래 `끄기`는 자동 컴파일·실행을 중지하고 `켜기`는 다시 켭니다. 코드를 수정하면 이전 실행을 취소합니다.
4. 실시간 출력 창에 초점을 둔 상태에서 F5는 재실행, Ctrl+P는 파일/프로젝트 모드, Ctrl+E는 출력·입력 인코딩(자동/UTF-8/CP949/CP1252) 전환입니다. 자동은 UTF-8을 우선하고 한국어 CP949를 보조 감지합니다. 표식 없는 인코딩은 모호할 수 있으므로 깨지면 직접 선택하세요. 여러 소스·헤더·라이브러리는 프로젝트 모드에서 모두 저장한 뒤 실행하세요.
5. 숨긴 창은 `도구 > CLive_Maro 출력 및 진단 창`, 업데이트는 `도구 > CLive_Maro 업데이트`입니다.

출력이 오면 무응답 시간을 갱신합니다. 출력 없이 30초간 계산하면 중지하되, 입력이 열린 저 CPU 대기·수면·스레드 I/O 대기, GDI가 있는 GUI 프로그램은 제외합니다. 입력 대기 감지는 보수적인 추정이며 다른 I/O 대기도 보호할 수 있습니다. 빈 입력은 Enter로 보낼 수 있습니다.

무응답이 확인된 경우만 오류로 표시합니다. 바로 실행되는 종료 조건 없는 빈 반복문은 위치를 표시하고, 위치를 확정할 수 없으면 `실행 무응답`으로 구분합니다. 정상 종료 후 출력이 없는 코드는 오류가 아닙니다.

자동 작업은 낮은 우선순위·CPU 상한 20%로 실행하며 변경 없는 코드는 다시 컴파일하지 않습니다. 출력 크기는 제한됩니다. 신뢰하는 코드만 실행하세요. 보안 샌드박스가 아니며 파일·네트워크·실행 메모리 접근을 제한하지 않습니다.

오른쪽 목록은 현재 문서의 선언/참조이며 전이 헤더 전체 목록은 아닙니다. 같은 원인으로 확인된 연쇄 오류만 묶고 독립 오류와 컴파일러 원문은 유지합니다.

프로젝트 모드는 MSBuild 17.8+와 C++ 도구가 필요하며 `.vcxproj`의 현재 구성·플랫폼을 사용합니다. 빌드는 최대 30분·8GiB, DLL/정적 라이브러리는 빌드만 합니다. CMake·NMake·원격 빌드는 지원하지 않습니다. 프로젝트 출력이 버퍼에 쌓이면 `fflush(stdout)` 또는 `setvbuf`를 사용하세요. 게임의 그래픽은 자체 창에서 실행됩니다. CLive 출력은 텍스트·줄 입력용이며 `_getch`, 콘솔 커서/색상, 그래픽 화면 내장은 지원하지 않습니다.
